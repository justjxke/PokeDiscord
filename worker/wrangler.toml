name = "poke-discord-bridge"
main = "src/index.ts"
compatibility_date = "2026-03-31"

# Set this to the HTTPS hostname that exposes your backend.
[vars]
POKE_BACKEND_ORIGIN = "https://mcs.creations.works/jake-mcp/"
