{
  "name": "poke-discord-bridge-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --import tsx --test test/proxy.test.ts",
    "typecheck": "../node_modules/.bin/tsc -p tsconfig.json --noEmit",
    "dev": "npx wrangler@latest dev",
    "deploy": "npx wrangler@latest deploy"
  }
}
